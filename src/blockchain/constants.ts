import Escrow from "../blockchain/abis/Escrow.json";
import RealState from "../blockchain/abis/RealState.json";

const escrowAddress = "esc";
const realStateAddress = "rea";
const escrowAbi = Escrow.abi;
const realStateAbi = RealState.abi;

export { escrowAddress, realStateAddress, escrowAbi, realStateAbi };
